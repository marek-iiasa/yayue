""" Place holder for the demo-model with export function (to be prepared by JG)."""
#
# very simple model, with hard-coded name, generating *.dll in the current dir
# e.g., example.dll

# there should be enough variables to define at least two criteria, preferably
# one min and one max
